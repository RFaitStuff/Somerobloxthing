# Enemies

Expected Studio asset shape:

- Enemy model names should match enemy ids or faction variants.
- Model includes `Humanoid` and `HumanoidRootPart`.
- Optional weapon/muzzle attachments can be added later for real ranged enemies.
- Server-only enemy stat and mission spawn data belongs in `src/ServerStorage/ServerData`.

This folder is not Rojo-managed.
